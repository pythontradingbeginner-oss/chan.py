# 青派缠论量化规则契约 v1

状态：P0 provisional  
适用范围：RB 15m 青派策略层，不改写 chan.py 的 K 线、笔、线段、中枢和 BSP 核心算法。

## 1. 文档目的

这份文档把教学语言转换为可编码、可测试和可追踪的规则。它不是对当前代码行为的描述：当前引擎输出只作为观察值，青派规则契约才是后续 P1-P6 的目标行为。

机器可读版本位于 `configs/qingpai_rule_contract_v1.json`。规则分为四种状态：

- `fixed`：教学文本足以确定，作为定义实现，不允许通过参数优化改写。
- `engineering_default`：安全优先的工程默认值，可以在独立研究配置中比较，但严格配置使用本文默认值。
- `research_candidate`：文本只给出原则，具体度量需用标注样本和滚动回测选择。
- `deferred`：不阻塞当前阶段，但必须在所属阶段启用前完成验证。

理论文本指纹：`SHA256 5C181AE58295CB1188323232E3C0596AD7B3056BFE1C357EDA13807188E45EC8`。

## 2. 不可配置的结构定义

### 2.1 中枢

1. 先有一笔或一段进入，随后连续三笔或三段必须存在共同重叠区。
2. 中枢方向只由进入方向决定：上涨进入偏多，下跌进入偏空。
3. 中枢起点是进入笔或进入段的起始极值，不是 `ZG`、`ZD` 或中轴。
4. 偏多中枢跌破起点、偏空中枢突破起点，原结构失效并重新分析。
5. 类中枢不进入 `qingpai_strict` 交易链。

原文依据：第 427-439、441-457、459-469 行。

### 2.2 走势分类

走势类型分为趋势与盘整；盘整分为延伸、扩张、扩展。趋势要求至少两个同向、震荡区不重叠的中枢。标准中枢后允许连续两笔或两段不接触原中枢；形成第二个中枢后必须转入扩张或扩展分类。达到九笔或九段属于中枢升级。

原文依据：第 473-479、503-539、549-605 行。

### 2.3 三买三卖

三买的回踩低点必须保持在 `ZG` 上方；三卖的反弹高点必须保持在 `ZD` 下方。重新进入中枢即不再是有效三买或三卖，不能退化为“跌到中轴仍算三买”或“涨到中轴仍算三卖”。

当前 chan.py 已有 `bsp3_back2zs()` 几何判断，但后续必须让策略层保存它使用的是哪个中枢及该中枢在信号时刻的快照。

## 3. 两类不同的止损

后续模型必须同时保存：

| 字段 | 含义 | 例子 |
|---|---|---|
| `setup_invalidation` | 整套分析前提失效 | 二买跌破关联一买低点；偏多中枢跌破进入起点 |
| `execution_stop` | 本次订单的小止损 | 二买回撤低点；三买回踩低点 |

二者不能继续挤在一个模糊的 `invalidation_price` 中。仓位按照更接近实际成交的 `execution_stop` 计算；结构状态由 `setup_invalidation` 管理。

## 4. 风险与执行默认值

1. 无有效结构止损、止损方向错误、风险预算不足一手时拒绝交易。
2. 单笔风险预算以决策时账户总权益计算，可用资金和保证金只限制最终可执行手数。
3. 入场信号只使用已收盘 K 线；保护止损按盘中触及执行。
4. 跳空越过止损时按下一可成交价格加滑点退出，不等待 K 线收盘。
5. `hard_blockers` 一票否决，评分只能在没有硬阻断的信号之间排序。
6. `max_loss_points` 保持累计已实现亏损限制，不得复用为单笔风险预算。

## 5. MACD 规则

结构与价格是主判断，MACD 是辅助：

1. 比较与笔方向相同的柱体绝对面积，不作正负代数抵消。
2. 面积优先，面积接近时比较峰值高度。
3. 价格力度默认以有效推进距离为主、单位 K 线斜率为辅；该度量是 P6 研究项。
4. 一根已收盘柱明显缩短产生候选，第二根继续缩短或颜色切换后确认。
5. 微创新高或新低同时 MACD 显著缩小，属于背驰候选，不是“只要创新高就否定背驰”。

原文依据：第 785-813、964-986、1082-1098、1214-1220、1344-1356 行。

## 6. 同级别与多级别

- 同级别分解不使用固定 N 笔切割，而是按确认的笔、中枢和关键结构转折持续分解，并寻找最合理的分解。
- 只允许修订尚未关闭的当前状态；已经生成的历史决策通过 revision 保留，不回写成另一笔交易。
- 多级别区间套按时间范围和 `available_at` 对齐，禁止直接比较不同级别独立计数的 `idx`。
- `qingpai_strict` 暂定父级别冲突或缺少必要次级别确认时拒绝开仓；减仓方案留到 P5 对照实验。

P4 将“走势分类”和“同级别交易视图”分成两层：底层状态机保留趋势、盘整、延伸、扩张、扩展五类原始关系；同级别视图仍以单一中枢为分析中心，在确认反向中枢或进入起点失效时关闭当前分解并开启新分解。

五类关系采用以下确定性工程规则：

1. 一个三笔标准中枢为盘整；仍围绕同一中枢运行、跨度超过三笔为延伸。
2. 两个同向中枢的核心区和波动区均不重叠，并按进入方向推进，为趋势。
3. 两个同向中枢核心区不重叠、波动区重叠，为扩张候选。
4. 扩张候选在突破第二中枢方向极值前先回撤触及前中枢，且不破第二中枢进入起点，开放状态修订为扩展。
5. 九笔或九段仅输出 `upgrade_candidate`；同级别视图不因此改写级别。

每次新建、修订、关闭都输出 `decomposition_id + revision + fingerprint`。开放状态可以追加 revision；关闭快照永久归档，后续行情不得回写。回测保存 `decomposition_transitions.csv`，三端决策追踪同时保存当前走势类型、方向和 revision。

## 7. RB 专属规则

- 研究结构使用调整连续序列，真实成交使用实际合约。
- 第一版换月平旧仓并重置结构，不继承原始合约价差形成的笔和中枢。
- 隔夜跳空越过保护止损时立即按第一可成交价格退出。
- RB 合约乘数由交易所合约配置提供；当前研究默认值为 10，但不能在策略逻辑中散落硬编码。

## 8. P0 标注样本

运行下面的命令可从真实 RB 15m 数据重建样本：

```bash
python scripts/build_qingpai_p0_samples.py
```

生成内容：

- `reports/qingpai_p0/*.png`：K 线、笔、中枢、进入方向、结构边界、BSP 和 MACD 标注图。
- `reports/qingpai_p0/index.md`：样本索引和当前引擎观察结果。
- `tests/fixtures/qingpai_p0/manifest.json`：信号时刻不可变结构快照。
- `tests/fixtures/qingpai_p0/rb_15m_windows.csv`：图例对应的真实 OHLC 窗口，含决策可见性标记。

样本图允许显示少量决策后的行情以帮助教学，但灰色区域明确标成 outcome only；机器决策只能读取 `is_visible_at_decision=true` 的行。

## 9. 尚未冒充成答案的事项

以下内容保留为后续阶段的显式决策，不阻塞 P1-P2：

1. 主动结构与被动支撑的完整状态枚举。
2. 父级别冲突时完全拒绝与减仓试错的收益比较。
3. 保本止损、分批止盈和对立中枢目标位的实际增益。

P0 的完成标准不是“所有交易细节永远不再变化”，而是每条定义、默认值和研究假设都有身份、依据、样本和所属阶段，后续代码不再依赖口头约定。

## 10. P1 决策内核追踪

P1 使用 `chan_futures.decision_pipeline.DecisionPipeline` 作为回测、CTA 和实盘的共享入口判定内核，并保留两种显式模式：

- `legacy`：保持历史回测可比性，继续按 grade 准入，不启用 hard blocker 一票否决。
- `qingpai_strict`：信号事件或评分缺失时关闭准入，要求确认状态，并执行 hard blocker 一票否决。

严格配置位于 `configs/rb_15m_qingpai_strict.yaml`。无论使用哪种模式，事件与评分不匹配、BSP 身份不匹配、交易方向不匹配和 `available_at` 晚于决策时刻都属于不可关闭的安全约束。回测结果通过 `signal_decisions` 保留接受与拒绝记录，调用 `BacktestResult.save()` 时写出 `signal_decisions.csv`；实盘入口同步写入 `SignalJournal`。

三买、三卖边界从 P1 起使用笔端结构极值 `SignalEvent.structural_price` 判断，确认 K 线收盘价 `reference_price` 只作为成交参考，不再替代回踩或反弹极值。P0 样本 `P0-RB15-007` 因结构极值回到 ZG 下方，在严格模式下产生 `hard_blocker:not_above_zs`。

## 11. P4 同级别分解追踪

P4 的实现位于 `strategy_policy/qingpai_decomposition.py`。它只读取 `CChan` 已计算出的笔、线段和中枢，冻结为不可变快照，不修改 `Bi/Seg/ZS` 核心算法。`RuntimeDecisionKernel` 在每根已收盘 K 线上更新分解状态，并把同一状态上下文附加到回测、vn.py CTA 和实盘的 `TradeIntent`。

P4 暂不按走势类型新增入场硬门槛。原因是趋势跟随、盘背反转和扩张/扩展的 BSP 允许集合尚需分别固化；本阶段先保证分类、修订边界和三端审计一致，避免用一个粗糙的“方向一致”条件误杀一买/一卖反转。
