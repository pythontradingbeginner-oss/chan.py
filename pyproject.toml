[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "chan.py"
version = "1.0.0"
description = "Chan theory quantitative analysis framework with a vn.py plugin."
requires-python = ">=3.10"
dependencies = [
    "numpy",
    "pandas",
]

[project.optional-dependencies]
vnpy = [
    "vnpy",
    "vnpy_sqlite",
]
gui = [
    "PySide6>=6.5",
]

[project.scripts]
chan = "chan_futures.cli:main"
chan-rb-plot-gui = "App.rb_chan_plot.ui:main"

[tool.setuptools.packages.find]
include = [
    "App*",
    "Bi*",
    "BuySellPoint*",
    "ChanModel*",
    "Combiner*",
    "Common*",
    "DataAPI*",
    "KLine*",
    "Math*",
    "Plot*",
    "Seg*",
    "ZS*",
    "chan_futures*",
    "data_foundation*",
    "vnpy_chan*",
]

[tool.setuptools.package-data]
"data_foundation.data" = ["static/*.csv"]
